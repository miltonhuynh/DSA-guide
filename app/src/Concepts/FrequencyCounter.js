import React from 'react'

export default function FrequencyCounter() {
  return (
    <div class="Concept_page">
      <div class="Concept_title">Frequency Counter</div>
      <div class="Concept_description">
        <div>This pattern uses objects or sets to collect values and frequencies of values.</div>
        <div>Used to avoid using nested loops or O(n^2) with arrays and strings</div>
      </div>
    </div>
  )
}
